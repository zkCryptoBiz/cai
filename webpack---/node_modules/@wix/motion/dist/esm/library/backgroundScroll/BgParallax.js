import { setCompHeightCSSVar } from '../../utils';
export default function create(options, dom) {
  if (dom) {
    setCompHeightCSSVar(dom);
  }
  const {
    speed = 0.2
  } = options.namedEffect;
  return [{
    ...options,
    part: 'BG_MEDIA',
    startOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 0
      }
    },
    endOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 100
      }
    },
    keyframes: [{
      transform: `translateY(-${100 * (1 - speed)}lvh)`
    }, {
      transform: `translateY(calc(var(--motion-comp-height, 100%) * ${1 - speed}))`
    }]
  }];
}
//# sourceMappingURL=BgParallax.js.map