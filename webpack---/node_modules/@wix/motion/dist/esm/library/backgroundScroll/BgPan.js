export default function create(options) {
  const {
    direction = 'left',
    speed = 0.2
  } = options.namedEffect;
  const offsetPercentage = 50 * speed / (1 + speed);
  let fromValue = `${offsetPercentage}%`;
  let toValue = `-${offsetPercentage}%`;
  if (direction === 'right') {
    [fromValue, toValue] = [toValue, fromValue];
  }
  return [{
    ...options,
    part: 'BG_MEDIA',
    startOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 0
      }
    },
    endOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 100
      }
    },
    keyframes: [{
      transform: `translateX(${fromValue})`
    }, {
      transform: `translateX(${toValue})`
    }]
  }];
}
//# sourceMappingURL=BgPan.js.map