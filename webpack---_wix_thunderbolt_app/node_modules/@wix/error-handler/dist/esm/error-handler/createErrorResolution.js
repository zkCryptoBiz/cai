import { ErrorResolution } from '../state/ErrorResolution';
export async function createErrorResolution(errorHandler, params) {
  const {
    translations: {
      getTranslateFn
    },
    onlineManager
  } = errorHandler;
  const {
    errorAccessor,
    errorCodesMap
  } = params;
  const errorResolution = new ErrorResolution({
    errorAccessor,
    errorCodesMap,
    getTranslateFn,
    onlineManager
  });
  await errorResolution.resolve();
  return errorResolution;
}
//# sourceMappingURL=createErrorResolution.js.map