export function typeToConverterSet(converterSets) {
    return converterSets.reduce((result, converterSet) => {
        const types = converterSet.types.reduce((typeResult, type) => {
            return Object.assign(Object.assign({}, typeResult), { [type]: converterSet });
        }, {});
        return Object.assign(Object.assign({}, result), types);
    }, {});
}
//# sourceMappingURL=utils.js.map